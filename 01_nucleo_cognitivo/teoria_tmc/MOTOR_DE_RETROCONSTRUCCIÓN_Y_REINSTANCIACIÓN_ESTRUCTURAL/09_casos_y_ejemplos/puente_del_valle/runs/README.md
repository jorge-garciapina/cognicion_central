# Runs

El único estado válido antes de localizar la fuente es `WAITING_SOURCE` o `PARTIAL` limitado al Kernel de Ingeniería. No registrar como fallo del kernel MRRE.

[RUN-MRRE-BRIDGE](run_v0_2_0.yaml) registra `WAITING_SOURCE` y enlaza el [CASE-MRRE-BRIDGE](../DOSSIER_OPERATIVO.md). No contiene grafos físicos inventados.
